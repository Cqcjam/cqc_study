<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:tx="http://www.springframework.org/schema/tx" xmlns:jdbc="http://www.springframework.org/schema/jdbc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
     http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.1.xsd
     http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
     http://www.springframework.org/schema/jdbc http://www.springframework.org/schema/jdbc/spring-jdbc-3.1.xsd
     http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.1.xsd
     http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-3.1.xsd">

    <!-- 导入属性配置文件 -->
    <context:property-placeholder
            location="classpath:application.properties"
            ignore-unresolvable="true"/>

    <!-- 添加使用注解依赖的配置 -->
    <context:annotation-config />

    <!-- 注解扫描的包 包括sub package子包 -->
    <context:component-scan base-package="com.cqc.unif.agency.*">
        <!--将Controller的注解排除掉 -->
        <context:exclude-filter type="annotation" expression="org.springframework.stereotype.Controller" />
    </context:component-scan>

    <bean id="agencyDataSource" class="com.mchange.v2.c3p0.ComboPooledDataSource"
          destroy-method="close">
        <property name="driverClass" value="${jdbc.agency.driverClassName}" />
        <property name="jdbcUrl" value="${jdbc.agency.url}" />
        <property name="user" value="${jdbc.agency.username}" />
        <property name="password" value="${jdbc.agency.password}" />
        <property name="minPoolSize" value="${jdbc.agency.min}" />
        <property name="maxPoolSize" value="${jdbc.agency.max}" />
        <property name="maxIdleTime" value="${jdbc.agency.IdleTime}" />
        <property name="idleConnectionTestPeriod" value="${jdbc.agency.idleConnectionTestPeriod}" />
    </bean>

    <!-- mybatis 配置 -->
    <bean id="agencySqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="agencyDataSource" />
        <property name="configLocation" value="classpath:config/mybatis/mybatis-config.xml" />
        <!-- 当Mapper文件跟对应的Mapper接口处于同一位置的时候可以不用指定该属性的值: -->
        <property name="mapperLocations"
                  value="classpath:com/cqc/unif/agency/persistence/*.xml" />
    </bean>


    <!-- Mapper接口扫描 -->
    <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <property name="basePackage" value="com.cqc.unif.agency.persistence;" />
        <property name="sqlSessionFactoryBeanName" value="agencySqlSessionFactory" />
    </bean>

</beans>